server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React SPA — send all non-asset routes to index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
